# The repository starts in deterministic fixture mode. No key is required.
AI_MODE=fixture

# Reserved for the server-only provider adapter. Do not expose this value with
# a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=
OPENAI_MODEL=

